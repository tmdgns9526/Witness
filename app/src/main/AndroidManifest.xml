<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.witness">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BucketList" android:label="버킷리스트 액티비티"/>
        <activity android:name=".HundredQnAFragment" android:label="백문백답 액티비티"/>
        <activity android:name=".AlbumMain" android:label="앨범 액티비티"/>
        <activity android:name=".AlbumDetail" android:label="앨범 상세 화면 액티비티"/>
    </application>

</manifest>